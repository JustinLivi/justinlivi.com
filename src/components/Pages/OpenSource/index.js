import CreateOpenSource from './containers';
import OpenSourceReducers from './reducers';

const OpenSource = CreateOpenSource();

export {
    CreateOpenSource,
    OpenSource,
    OpenSourceReducers,
};

export default {
    CreateOpenSource,
    OpenSource,
    OpenSourceReducers,
};
