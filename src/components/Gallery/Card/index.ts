import CardComponent from './component';
import CreateCardContainer from './container';
import CardReducers from './reducers';

const Card = CreateCardContainer();

export { Card, CardComponent, CreateCardContainer, CardReducers };
