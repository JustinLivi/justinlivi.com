import { ColorTheme } from 'styled-components';

export const lightTheme: ColorTheme = {
  // text, etc
  primary: '',
  // backgrounds
  secondary: '#fff',

  // other colors
  brinkbitBlue: '#60aecc',
};
