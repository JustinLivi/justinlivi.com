import { ColorTheme } from 'styled-components';

export const darkTheme: ColorTheme = {
  // text, etc
  primary: '',
  // backgrounds
  secondary: '',

  // other colors
  brinkbitBlue: '#60aecc',
};
